const jokeTasks = require('./joke-tasks')
const sentencesTasks = require('./sentences-tasks')
const transcriptionTasks = require('./transcription-tasks')
const auth = require('./auth')
const users = require('./users')
const ownDictionaries = require('./own-dictionaries')
const textTranscripton = require('./text-transcription')


module.exports = {
  jokeTasks,
  sentencesTasks,
  transcriptionTasks,
  auth,
  users,
  ownDictionaries,
  textTranscripton,
}
